import java.sql.Timestamp;
import java.util.Date;

public class TimeStamp {
    Timestamp stamp = new Timestamp(1489575600);
    Date date = new Date(stamp.getTime());
  //      System.out.print        Timestamp stamp = new Timestamp(1489575600);
  //  Date date = new Date(stamp.getTime());
   //     System.out.println(date);ln(date);
}
